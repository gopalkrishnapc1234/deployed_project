<nav class="navbar navbar-expand-lg navbar-light bg-dark text-white shadow-sm">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/"> GradMate</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navMenu">
      <ul class="navbar-nav ms-auto align-items-center">
        <% if (!currentUser) { %>
          <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
          <li class="nav-item"><a class="btn btn-success ms-2" href="/register">Register</a></li>
        <% } else { %>
          <% if (currentUser.role === 'user') { %>
            <li class="nav-item"><a class="nav-link" href="/user/dashboard">Dashboard</a></li>
            <li class="nav-item"><a class="nav-link" href="/user/jobs">Jobs</a></li>
          <% } else if (currentUser.role === 'admin') { %>
            <li class="nav-item"><a class="nav-link" href="/admin">Admin Panel</a></li>
          <% } %>
          <li class="nav-item"><a class="btn btn-outline-danger ms-3" href="/logout">Logout</a></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
